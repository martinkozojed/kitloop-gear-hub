HARNESS_ENV=local
ENVIRONMENT=local
REQUIRE_E2E_HARNESS=true
E2E_SEED_TOKEN=super_secret_e2e_token
SUPABASE_URL=http://127.0.0.1:54321
# Paste the ES256 service_role JWT from `supabase status` here (local dev only — never a production key)
SUPABASE_SERVICE_ROLE_KEY=
