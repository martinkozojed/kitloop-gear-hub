{
  "lock": "deno.lock",
  "nodeModulesDir": "none",
  "tasks": {
    "lock": "deno cache --lock=deno.lock supabase/functions/**/*.ts scripts/**/*.ts",
    "deno:cache": "deno cache --lock=deno.lock --frozen supabase/functions/**/*.ts scripts/**/*.ts",
    "deno:test": "deno test --allow-env --allow-read --allow-import=deno.land,esm.sh,legacy.esm.sh --lock=deno.lock --frozen supabase/functions"
  }
}